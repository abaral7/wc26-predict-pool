export const metadata = {
  title: "World Cup 2026 Pool",
  description: "Prediction pool standings, matches and money",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0 }}>{children}</body>
    </html>
  );
}
